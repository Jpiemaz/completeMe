commented
<% if activity.trackable %>
  <strong><%= activity.trackable.body %></strong>
  on <%= link_to activity.trackable.task.name, activity.trackable.task %>
<% else %>
  on a task that has since been removed
<% end %>
